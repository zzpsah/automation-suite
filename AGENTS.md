# AGENTS.md

This repository is being used as a project-side Development OS integration test.

For DevOS-managed work, read the reusable Development OS bootstrap protocol and this project's `.ai/` durable context before material technical conclusions.

Repository source + Git are implementation authority. `.ai` is portable project context. `STATE-INDEX.md` is deterministic evidence only.
