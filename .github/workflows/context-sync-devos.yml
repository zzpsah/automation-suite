name: DevOS Context Sync Integration Test

on:
  push:
    branches:
      - devos-p10-context-sync-test

permissions:
  contents: write

jobs:
  sync:
    uses: zzpsah/chatgpt-development-os/.github/workflows/context-sync-contract-smoke.yml@main
    with:
      context_directory: .ai
      meaningful_path_patterns: 'src/**,app/**,package.json,.github/workflows/**'
    permissions:
      contents: write
