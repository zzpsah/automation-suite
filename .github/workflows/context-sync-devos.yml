name: DevOS Context Sync Integration Test

on:
  push:
    branches:
      - devos-p10-context-sync-test

permissions:
  contents: read

jobs:
  smoke:
    uses: zzpsah/chatgpt-development-os/.github/workflows/reusable-smoke.yml@main
    permissions:
      contents: read
